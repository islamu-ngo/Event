<span hidden data-blazor-interactive="@(_isInteractive ? "true" : "false")"></span>

@code {
    private bool _isInteractive;

    protected override void OnAfterRender(bool firstRender)
    {
        if (!firstRender)
        {
            return;
        }

        _isInteractive = true;
        StateHasChanged();
    }
}
