name: Release Adapter Preview Discovery

on:
  push:
  pull_request:

jobs:
  release-adapter-preview:
    runs-on: linux
    steps:
      - run: |
          printf '%s\n' 'release-adapter-preview: no-op-safe Tangled discovery'
