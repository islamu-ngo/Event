# ABOUTME: Records instance-owned Local credential issuance and private first-use replacement.
# ABOUTME: Documents operation recovery, schema requirements, and current-session authority.
Change-Id: CHG-01M1SMGJ33KVXHCN9BHBTMM9ME
Title: secure administrative Local credential handover
Type: feat
Scope: access
Summary: Persist credential-operation receipts, require private first-use replacement, and revoke stale Local sessions across colocated and external Identity stores.
Group: email-optional-self-hosting
Supersedes: []
Impacts:
  Breaking:
    Reference: docs/internal/API_CHANGELOG.md
    Disposition: documented
    Detail: Local credentials require current lifecycle state and security-stamp validation; old unfenced sessions are not accepted.
  Security:
    Reference: docs/internal/SECURITY-MODEL.md
    Disposition: documented
    Detail: Current persisted instance authority governs temporary credential issuance, one-time handover and operation recovery; private replacement grants no ordinary session.
  Migration:
    Reference: docs/internal/AUTHENTICATION.md
    Disposition: documented
    Detail: The credential-operation ledger requires generated migrations in both primary and external Identity stores for every supported provider.
  Configuration:
    Reference: docs/internal/AUTHENTICATION.md
    Disposition: not-applicable
    Detail: The credential lifecycle reuses the selected Identity topology and existing secret authority without adding configuration keys.
  OpenAPI:
    Reference: docs/internal/API_CHANGELOG.md
    Disposition: documented
    Detail: Generated contracts expose instance Local account administration, safe operation status and reconciliation, and restricted first-use credential replacement.
  Operator:
    Reference: docs/public/documentation/readme/administration-and-branding/admin-guide.md
    Disposition: documented
    Detail: Administrators hand over temporary credentials once and retain exact operation references after uncertain outcomes; lost passwords require a new reset rather than response replay.
