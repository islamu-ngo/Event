name: Release Adapter Final Discovery

on:
  manual:

jobs:
  release-adapter-final:
    runs-on: linux
    steps:
      - run: |
          printf '%s\n' 'release-adapter-final: transport-only no-checkout discovery'
