name: Release Adapter Preview Discovery

on:
  push:
  pull_request:

jobs:
  release-adapter-preview:
    name: release-adapter-preview
    runs-on: self-hosted
    steps:
      - name: Report preview adapter contract
        run: |
          printf '%s\n' 'release-adapter-preview: no-op-safe read-only discovery'
